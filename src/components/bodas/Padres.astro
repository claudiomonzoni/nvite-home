---
interface Props {
  mamaNovia: string;
  papaNovia: string;
  mamaNovio: string;
  papaNovio: string;
}

const { mamaNovia, papaNovia, mamaNovio, papaNovio } = Astro.props;
---

<div class="bande">
  <h2>Nuestros Padres</h2>
  <div class="conte">
    <ul class="padre">
      <hr />
      <h3>Novia</h3>
      <li>{mamaNovia}</li>
      <li>{papaNovia}</li>
    </ul>
    <ul class="padre">
      <hr />
      <h3>Novio</h3>
      <li>{mamaNovio}</li>
      <li>{papaNovio}</li>
    </ul>
  </div>
</div>

<style lang="scss">
 @use "../../estilos/_variables.scss" as *;
  h2 {
    text-align: center;
    font-family: $garigol, serif;
    font-size: clamp(1.8rem, 2vw, 3rem);
    
  }
  .bande {
    background: rgb(254, 241, 241);
    background: linear-gradient(0deg, var(--terciario) -10%, rgba(255, 255, 255, 0) 40%);
    border-radius: $radio;
    font-size: clamp(1.5rem, 1.7vw, 2.4vw);
    z-index: -2;
    padding-top: $margen;

  }
  .conte {
    display: flex;
    justify-content: space-evenly;
    align-items: center;
    padding: $margen;
    position: relative;
    @include cel {
      flex-wrap: wrap;
      text-align: center;
      .padre:last-child {
        margin-top: $margen-cel;
      }
    }
  }
  @include cel{
    li{
      padding: $margen-min 0 0 0;
    }
  }
  hr {
    position: absolute;
    left: 0;
    top: 8%;
    width: 100%;
    z-index: -1;
    @include cel {
      top: 2%;
    }
  }
  .padre {
    position: relative;

    h3 {
      //   background-color: #fff;
      color: var(--acento);
      display: inline-block;
      background-color: transparentize($color: #fff, $amount: 0);
      background-blend-mode: difference;
      backdrop-filter: blur(20%);
      border-radius: 4px;
      padding: 0em 0.6em 0 0;
      @include cel {
        padding: 0em 0.6em;
      }
    }
    // flex: 1 0 fit-content;
    width: fit-content;
  }
</style>